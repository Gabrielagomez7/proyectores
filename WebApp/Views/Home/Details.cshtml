@model Proyector;
@{
    ViewData["Title"] = "Detalles del proyector";
}

<div class="container">
    <h2 class="display-6 mb-3">Información del Proyector # @Model.Id</h2>


    <div class="display-6 mb-3 mb-5 fs-5">

        <div class="form-group">
            <label class="form-label">Marca:</label>
            <label class="form-label">@Model.Marca</label>
        </div>
     
        <div class="form-group">
            <label class="form-label">Modelo:</label>
            <label class="form-label">@Model.Modelo</label>
        </div>

        <div class="form-group">
            <label class="form-label">Numero de Serie:</label>
            <label class="form-label">@Model.NumeroDeSerie</label>
        </div>

        <div class="form-group">
            <label class="form-label">Situacion del Proyector:</label>
            <label class="form-label">@Model.Situacion</label>
        </div>

        <div class="form-group">
            <label class="form-label">Fecha de Alta:</label>
            <label class="form-label">@Model.FechaDeAlta</label>
        </div>

        <div class="form-group">
            <label class="form-label">Fecha de Baja:</label>
            <label class="form-label">@Model.FechaDeBaja</label>
        </div>

        <div class="form-group">
            <a class="btn btn-secondary" asp_controller="Home" asp-action="Index">Cancelar</a>
        </div>

    </div>
</div>